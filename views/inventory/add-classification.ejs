<main class="classification-view">
    <h1><%= title %></h1>
    <%- messages() %>
    <% if (errors) { %>
        <ul class="notice">
            <% errors.array().forEach(error => { %>
                <li><%= error.msg %></li>
            <%  }) %>
        </ul>
    <% } %>

    <div class="form-container">
        <form action="/inv/addClassification" method="post">
            <p>NOTE: Classification name cannot contain spaces or special characters.</p>
            <fieldset>
                <label for="classification_name">Classification Name</label>
                <input type="text" name="classification_name" id="classification_name" 
                required pattern="^[a-zA-Z0-9]+$" title="No spaces or special characters allowed.">
                
                <button type="submit" class="btn-submit">Add Classification</button>
            </fieldset>
        </form>
    </div>
</main>